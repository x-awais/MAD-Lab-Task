var student = {
    name : "David Rayy",
    sclass : "VI",
    rollno : 12 };
   
    
    if (student.hasOwnProperty("name","sclass","rollno")) {
        console.log(student.name,student.sclass,student.rollno);
      }
